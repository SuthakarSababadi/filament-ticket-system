<?php

namespace App\Services;

use Illuminate\Database\Eloquent\Collection;


class TextMessageService{

    public function sendMessage(array $data, Collection $records)
    {
       // $textMessages = collect[];

        // $records ->map(function($records) use ($data, $textMessages) {

        // })
    }
}
